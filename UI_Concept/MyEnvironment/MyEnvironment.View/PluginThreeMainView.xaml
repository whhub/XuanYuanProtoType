<UserControl x:Class="MyEnvironment.View.PluginThreeMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Border BorderThickness="10" Background="Azure">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding CalculateResult}" TextAlignment="Center" FontSize="23" Grid.Row="0"></TextBlock>
        <Button Command="{Binding CalculateCmd}" Content="Calculate" Grid.Row="1"></Button>
    </Grid>
    </Border>
</UserControl>
